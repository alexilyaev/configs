{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": ["./base.json"],
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "plugins": [{ "name": "next" }]
  },
  // Note that extending this config without defining `include` in your
  // `tsconfig.json` fails Next.js `dev`, `build`, and `lint` commands.
  // So, you must copy this `include` array to your `tsconfig.json`
  // but without the `${configDir}/` prefix.
  // ... GitHub Issue: TBD
  "include": [
    "${configDir}/**/*.ts",
    "${configDir}/**/*.mts",
    "${configDir}/**/*.tsx",
    "${configDir}/**/*.json",
    "${configDir}/src/env.js",
    "${configDir}/next-env.d.ts",
    "${configDir}/.next/types/**/*.ts"
  ],
  "exclude": ["${configDir}/node_modules", "${configDir}/.next"]
}
